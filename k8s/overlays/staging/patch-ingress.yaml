apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ledgora
spec:
  tls:
    - hosts:
        - app.staging.ledgora.com
      secretName: ledgora-staging-tls
  rules:
    - host: app.staging.ledgora.com
