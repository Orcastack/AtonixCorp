apiVersion: v1
kind: ConfigMap
metadata:
  name: ledgora-config
data:
  DJANGO_ALLOWED_HOSTS: "api.ledgora.com"
  CORS_ALLOWED_ORIGINS: "https://app.ledgora.com"
  CSRF_TRUSTED_ORIGINS: "https://app.ledgora.com"
  FRONTEND_BASE_URL: "https://app.ledgora.com"
  APPROVAL_NOTIFICATION_BASE_URL: "https://app.ledgora.com"
  DEPLOYMENT_ENVIRONMENT: production
  APP_VERSION: production
