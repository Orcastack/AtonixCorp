---
# roles/lgx-common/defaults/main.yml
# Baseline defaults for all AtonixCorp hosts.

atc_timezone: UTC
atc_ntp_servers:
  - 0.pool.ntp.org
  - 1.pool.ntp.org

atc_log_dir: /var/log/atonixcorp
atonixcorp_app_user: atonixcorp
atonixcorp_app_group: atonixcorp

atc_security_packages:
  - unattended-upgrades
  - aide
  - auditd
  - fail2ban

atc_required_packages:
  - curl
  - wget
  - jq
  - ca-certificates
  - gnupg
  - lsb-release
  - python3-pip
  - docker.io
  - docker-compose-plugin
